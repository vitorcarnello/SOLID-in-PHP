<?php

require "AccountType.php";

class Debit
{	
	function toDebit($value, $accountType){
		if($accountType == CHECKING_ACCOUNT){
			// do something
		}

		if($accountType == SAVING_ACCOUNT){
			//do something
		}
	}
}